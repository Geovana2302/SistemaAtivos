<!DOCTYPE html>
<html lang="pt-br">
<head>
    <meta charset="utf-8" />
    <meta name="viewport" content="width=device-width, initial-scale=1.0" />
    <title>@ViewBag.Title - Sistema de Ativos</title>

    <link rel="stylesheet" href="~/lib/bootstrap/bootstrap.min.css" />
    <link rel="stylesheet" href="~/css/site.css" asp-append-version="true" />
    <link rel="stylesheet" href="https://cdn.jsdelivr.net/npm/bootstrap-icons@1.11.3/font/bootstrap-icons.min.css" />

    <style type="text/css" media="print">
        @@page { margin: 0; }
        body { padding: 15mm !important; }
        nav, footer, .no-print { display: none !important; }
        *, *::before, *::after {
            -webkit-print-color-adjust: exact !important;
            color-adjust: exact !important;
            print-color-adjust: exact !important;
        }
    </style>
</head>
<body>
    @{
        var usuarioId = Context.Session.GetInt32("UsuarioId");
        var tipo      = Context.Session.GetString("UsuarioTipo");
        var nome      = Context.Session.GetString("UsuarioNome");
        var empId     = Context.Session.GetInt32("EmpresaId");
    }

    @if (usuarioId != null)
    {
        <nav class="navbar navbar-expand-md" style="background:#fff; border-bottom:1px solid #E5E4E0;" aria-label="Menu Principal">
            <div class="container-fluid px-3">

                <a class="navbar-brand d-flex align-items-center gap-2 text-decoration-none" href="#" aria-label="Sistema de Ativos">
                    <div style="width:30px;height:30px;background:#534AB7;border-radius:6px;display:flex;align-items:center;justify-content:center;color:#fff;font-size:16px;font-weight:500;" aria-hidden="true">A</div>
                    <span style="font-size:14px;font-weight:500;color:#222;">Sistema Ativos</span>
                </a>

                <button class="navbar-toggler" type="button"
                        data-bs-toggle="collapse" data-bs-target="#menu-principal"
                        aria-controls="menu-principal" aria-expanded="false"
                        aria-label="Abrir menu de navegação">
                    <span class="navbar-toggler-icon"></span>
                </button>

                <div class="collapse navbar-collapse" id="menu-principal">

                    <ul class="navbar-nav menu-absoluto gap-4 mb-2 mb-md-0">
                        @if (tipo == "Admin")
                        {
                            <li class="nav-item"><a class="nav-link" asp-controller="Admin" asp-action="Dashboard" aria-label="Acessar página de Empresas">Empresas</a></li>
                            <li class="nav-item"><a class="nav-link" asp-controller="Admin" asp-action="Admins" aria-label="Acessar página de Administradores">Administradores</a></li>
                            <li class="nav-item"><a class="nav-link" asp-controller="Ativo" asp-action="Index" aria-label="Acessar lista de Ativos">Ativos</a></li>
                            <li class="nav-item"><a class="nav-link" asp-controller="Ativo" asp-action="Inativos" aria-label="Acessar lista de Inativos">Inativos</a></li>
                        }
                        else
                        {
                            <li class="nav-item"><a class="nav-link" asp-controller="Admin" asp-action="Empresa" asp-route-id="@empId" aria-label="Acessar Minha Empresa">Minha Empresa</a></li>
                            <li class="nav-item"><a class="nav-link" asp-controller="Ativo" asp-action="Index" aria-label="Acessar lista de Ativos">Ativos</a></li>
                            <li class="nav-item"><a class="nav-link" asp-controller="Ativo" asp-action="Inativos" aria-label="Acessar lista de Inativos">Inativos</a></li>
                        }
                    </ul>

                    <ul class="navbar-nav ms-auto align-items-center gap-3 mb-2 mb-md-0">
                        <li class="nav-item d-flex align-items-center gap-2" style="padding:0 4px">
                            <span style="font-size:13px;color:#555;" aria-label="Usuário logado: @nome">@nome</span>
                            <span class="@(tipo == "Admin" ? "badge-admin" : "badge-cliente")"
                                  style="font-size:10px;font-weight:500;padding:2px 8px;border-radius:20px;text-transform:uppercase;"
                                  aria-label="Nível de acesso: @tipo">
                                @(tipo == "Admin" ? "Admin" : "Gestor")
                            </span>
                        </li>
                        <li class="nav-item">
                            <a class="btn-logout" asp-controller="Account" asp-action="Logout" aria-label="Sair do sistema">Sair</a>
                        </li>
                    </ul>

                </div>
            </div>
        </nav>
    }

    <div class="@(usuarioId != null ? "main-content" : "")">
        @if (TempData["Sucesso"] != null)
        {
            <div class="alert-custom alert-success-custom" role="alert" aria-live="polite">@TempData["Sucesso"]</div>
        }
        @if (TempData["Erro"] != null)
        {
            <div class="alert-custom alert-danger-custom" role="alert" aria-live="assertive">@TempData["Erro"]</div>
        }
        @RenderBody()
    </div>

    <script src="~/lib/jquery/jquery.min.js"></script>
    <script src="~/lib/bootstrap/bootstrap.bundle.min.js"></script>

    @await RenderSectionAsync("Scripts", required: false)
</body>
</html>
