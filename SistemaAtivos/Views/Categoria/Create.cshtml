@model SistemaAtivos.Models.Categoria
@{
    ViewBag.Title = "Nova Categoria";
    var isAdmin = Session["UsuarioTipo"]?.ToString() == "Admin";
}

<div class="page-header">
    <h4 class="page-title">Nova Categoria</h4>
</div>

<div class="form-card">
    @using (Html.BeginForm())
    {
        @Html.AntiForgeryToken()
        @Html.ValidationSummary(true, "", new { @class = "alert-custom alert-danger-custom" })

        <div class="field-group">
            @Html.LabelFor(m => m.Nome, new { @class = "field-label" })
            @Html.TextBoxFor(m => m.Nome, new { @class = "field-input", placeholder = "Ex: Computadores, Moveis..." })
            @Html.ValidationMessageFor(m => m.Nome, "", new { @class = "field-error" })
        </div>

        <div class="field-group">
            @Html.LabelFor(m => m.Descricao, new { @class = "field-label" })
            @Html.TextAreaFor(m => m.Descricao, new { @class = "field-input", rows = "3", placeholder = "Descrição da categoria (opcional)" })
        </div>

        if (isAdmin && ViewBag.EmpresaPreSelecionada == true)
        {
            @Html.HiddenFor(m => m.EmpresaId)
        }
        else if (isAdmin)
        {
            <div class="field-group">
                <label class="field-label">Empresa</label>
                @Html.DropDownList("EmpresaId", (SelectList)ViewBag.Empresas, "-- Selecione --", new { @class = "field-input" })
            </div>
        }

        <div class="form-actions">
            @Html.ActionLink("Cancelar", "Index", null, new { @class = "btn-cancel" })
            <button type="submit" class="btn-purple">Criar Categoria</button>
        </div>
    }
</div>